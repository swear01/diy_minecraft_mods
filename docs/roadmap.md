# Roadmap

## Backlog

- `Karst_terrian` 與 `Magic_Storage` 各自的 mod 功能實作(細節見各專案 docs/PLAN)。
- 資源包持續擴充。

## Recently Done

- 根 repo 與 `Magic_Storage` 套用 agent-rules(AGENTS.md + docs/)。
- 將 skills 遷移至 shared-skills shelf(skillshare-reveal)。
- 新增 repository README,文件化單一 `resource-packs/` 佈局。
